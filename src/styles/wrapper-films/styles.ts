import styled, { css } from 'styled-components';

export const WrapperFilmes = styled.div`
    ${() => css`
        width: 100%;
        height: 100%;
        overflow: auto;
        padding-bottom: 5%;
    `}
`;
